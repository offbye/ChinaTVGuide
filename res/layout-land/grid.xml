<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:id="@+id/home_view" android:layout_width="fill_parent"
	android:layout_height="fill_parent" android:orientation="vertical"
	android:spacing="5dp" android:background="@drawable/homelarge">

	<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
		android:layout_width="fill_parent" android:padding="15dp"
		android:background="@drawable/topbg" android:layout_height="wrap_content"
		android:focusable="false">
		<EditText android:id="@+id/SearchText" android:background="@drawable/textfield_search"
			android:layout_width="fill_parent" android:padding="5dp"
			android:layout_margin="0dp" android:layout_toLeftOf="@+id/icon_search"
			android:layout_height="wrap_content" android:singleLine="true"
			android:hint="@string/search_hint" />
		<ImageButton android:layout_width="wrap_content"
			android:padding="5dp" android:layout_margin="0dp" android:id="@+id/search"
			android:layout_height="wrap_content" android:src="@drawable/ic_btn_search"
			android:background="@drawable/btn_search_dialog"
			android:layout_alignParentRight="true" />
	</RelativeLayout>
	<GridView android:id="@+id/myGrid" android:layout_width="fill_parent"
		android:layout_height="fill_parent" android:padding="10dp"
		android:verticalSpacing="10dp" android:horizontalSpacing="15dp"
		android:numColumns="4" android:columnWidth="70dp" android:stretchMode="columnWidth"
		android:gravity="center" />
</LinearLayout>